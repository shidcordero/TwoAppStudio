<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item android:id="@+id/itemSort"
        android:title="@string/sort"
        android:icon="@drawable/ic_sort"
        app:showAsAction="ifRoom"
        android:enabled="true">

        <menu>
            <item android:id="@+id/itemSortAscending"
                android:title="@string/sort_ascending"
                app:showAsAction="ifRoom"/>

            <item android:id="@+id/itemSortDescending"
                android:title="@string/sort_descending"
                app:showAsAction="ifRoom"/>
        </menu>
    </item>

    <item android:id="@+id/itemDelete"
        android:title="@string/delete"
        android:icon="@drawable/ic_delete"
        android:visible="false"
        app:showAsAction="ifRoom"
        android:enabled="true"/>
</menu>